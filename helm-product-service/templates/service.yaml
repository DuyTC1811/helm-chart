apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-product-service.fullname" . }}
  labels:
    {{- include "helm-product-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-product-service.selectorLabels" . | nindent 4 }}
