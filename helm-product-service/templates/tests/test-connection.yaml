apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-product-service.fullname" . }}-test-connection"
  labels:
    {{- include "helm-product-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-product-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
